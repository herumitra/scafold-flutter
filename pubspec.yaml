name: vimedika
description: "A new Flutter project."
# pub.dev using `flutter pub publish`. This is preferred for private packages.
# publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0


dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0

  #text___________________________________________
  google_fonts: ^4.0.5
  #icons__________________________________________
  cupertino_icons: ^1.0.8
  hugeicons: ^0.0.7
  icons_plus: ^5.0.0
  flutter_launcher_icons: ^0.14.3
  #animation______________________________________
  animated_splash_screen: ^1.3.0
  animated_text_kit: ^4.2.3
  animated_login: ^1.7.2
  easy_sidemenu: ^0.6.1
  page_transition: ^2.2.1
  lottie: ^3.3.1
  #layout & style_________________________________
  responsive_framework: ^1.5.1
  flutter_staggered_grid_view: ^0.7.0
  crud_table: ^0.0.6
  flutter_layout_grid: ^2.0.7
  syncfusion_flutter_datagrid: ^28.2.9
  easy_form_kit: ^5.0.0+1
  #storage & Api__________________________________
  provider: ^6.1.2
  dio: ^5.8.0+1
  shared_preferences: ^2.5.1
  websafe_svg: ^3.0.1+10
  #properties_____________________________________
  persistent_shopping_cart: ^0.0.8
  flutter_easyloading: ^3.0.5
  printing: ^5.14.2
  flutter_svg: ^2.0.17
  flutter_native_splash: ^2.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
  - assets/
  - assets/icons/
  - assets/images/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
  adaptive_icon_foreground: "assets/images/logo.png"
  adaptive_icon_background: "#ffffff"  # Bisa ganti warna sesuai tema kamu
